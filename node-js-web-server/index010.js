
// In order to create your first server, you need the `http` module.
// The code const fs = require('fs'); includes the `fs` module and assigns
// it th the variable `fs`. Include the `http` module and assign it to a
// variable named `http`.
