const http = require('http');

http.createServer(function () {
   
});

// The callback function in `createServer` takes two parameters, `request` and `response`.
// Pass the two parameters to the callback function like so:
// ```
// http.createServer(function(req, res) {

// });
// ```