const http = require('http');


// After including a module, you can use any of the methods attached to it.
// For example, `http.Agent();` uses the `Agent` method. Use the `http` module's
// `createServer` method to build a server.
 